# Embeddings

Story embeddings represent article content in vector space.

Embeddings enable:

semantic similarity
topic clustering
story deduplication

Possible models:

OpenAI embedding models
local embedding models
sentence-transformer models